# app/answer/__init__.py
"""Дополнительный слой обоснованного ответа для rag_api (самодостаточный модуль).

Всё для функции здесь: schema, config, generator, router (маршрутизатор),
pipeline (оркестратор) и маршрут /answer. Подключается из main.py одной строкой
app.include_router(answer.router). Оригинальные файлы rag_api не меняются, поэтому
`git pull` rag_api проходит без конфликтов, а всю функцию можно добавить или удалить
одной папкой.
"""
